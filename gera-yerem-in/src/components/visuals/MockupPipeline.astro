<div class="mockup-pipeline">
    <div class="pipeline-track">
        <!-- Connection Line -->
        <div class="pipeline-line"></div>
        
        <!-- Nodes -->
        <div class="node node-start">
            <div class="node-icon"></div>
            <div class="node-label"></div>
        </div>
        <div class="node node-middle">
            <div class="node-icon"></div>
            <div class="node-label"></div>
        </div>
        <div class="node node-end">
            <div class="node-icon"></div>
            <div class="node-label"></div>
        </div>

        <!-- Moving Packet -->
        <div class="data-packet"></div>
    </div>
</div>

<style>
    .mockup-pipeline {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }

    .pipeline-track {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
        max-width: 240px;
        position: relative;
    }

    .pipeline-line {
        position: absolute;
        top: 50%;
        left: 0;
        right: 0;
        height: 2px;
        background: #e2e8f0;
        transform: translateY(-50%);
        z-index: 0;
    }

    .node {
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 6px;
    }

    .node-icon {
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background: #ffffff;
        border: 2px solid #cbd5e1;
        transition: border-color 0.3s ease;
    }

    .node-label {
        width: 30px;
        height: 4px;
        background: #e2e8f0;
        border-radius: 2px;
    }

    /* Moving Packet Animation */
    .data-packet {
        position: absolute;
        top: 50%;
        left: 0;
        width: 8px;
        height: 8px;
        background: var(--color-accent);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        box-shadow: 0 0 8px var(--color-accent);
        z-index: 2;
        animation: flowPacket 3s infinite linear;
    }

    @keyframes flowPacket {
        0% { left: 0%; opacity: 0; }
        10% { opacity: 1; }
        90% { opacity: 1; }
        100% { left: 100%; opacity: 0; }
    }

    /* Node pulse when packet hits */
    .node-start .node-icon { animation: pulseNode 3s infinite 0s; }
    .node-middle .node-icon { animation: pulseNode 3s infinite 1.5s; }
    .node-end .node-icon { animation: pulseNode 3s infinite 3s; }

    @keyframes pulseNode {
        0%, 90%, 100% { border-color: #cbd5e1; transform: scale(1); }
        10% { border-color: var(--color-accent); transform: scale(1.1); }
    }
</style>
