<div class="mockup-portal">
    <div class="portal-card">
        <div class="avatar-circle"></div>
        <div class="input-line"></div>
        <div class="input-line short"></div>
        <div class="login-btn">
            <span class="btn-text"></span>
        </div>
        
        <!-- Verified Badge Overlay -->
        <div class="verified-badge">
            <svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                <path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </div>
    </div>
</div>

<style>
    .mockup-portal {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        /* Removed background gradient to let BentoCard background show through or use transparency */
    }

    .portal-card {
        width: 120px;
        background: linear-gradient(145deg, #2c2c2e, #1c1c1e);
        padding: 20px;
        border-radius: 16px;
        box-shadow: 
            0 14px 34px -8px rgba(0, 0, 0, 0.6),
            0 0 0 1px rgba(255, 255, 255, 0.1),
            inset 0 1px 0 rgba(255, 255, 255, 0.05);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
        position: relative;
    }

    .avatar-circle {
        width: 32px;
        height: 32px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        margin-bottom: 4px;
        border: 1px solid rgba(255, 255, 255, 0.05);
    }

    .input-line {
        width: 100%;
        height: 6px;
        background: rgba(255, 255, 255, 0.05); /* Very subtle input bg */
        border-radius: 3px;
    }
    .input-line.short {
        width: 70%;
    }

    .login-btn {
        width: 100%;
        height: 20px;
        background: var(--color-accent);
        border-radius: 4px;
        margin-top: 4px;
        opacity: 0.8; 
    }

    .verified-badge {
        position: absolute;
        top: -8px;
        right: -8px;
        width: 24px;
        height: 24px;
        background: #34c759; /* Apple Success Green */
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 2px 8px rgba(0,0,0,0.3);
        border: 2px solid #1c1c1e; /* Match card bg */
        animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        transform: scale(0);
        animation-delay: 0.5s;
    }

    .check-icon {
        width: 14px;
        height: 14px;
        color: white;
    }

    @keyframes popIn {
        to { transform: scale(1); }
    }
</style>
