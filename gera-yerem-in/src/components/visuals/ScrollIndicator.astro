---
/* 
  Sleek Scroll Indicator
  - Minimalist "Mouse" design
  - Smooth scrolling wheel animation
  - Fades out on scroll (handled via CSS/JS if needed, but simple loop for now)
*/
---

<div class="scroll-indicator" aria-hidden="true">
  <div class="mouse">
    <div class="wheel"></div>
  </div>
  <div class="arrows">
    <span></span>
    <span></span>
    <span></span>
  </div>
</div>

<style>
  .scroll-indicator {
    position: absolute;
    bottom: 6vh; /* Relative to viewport height for safety */
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    opacity: 0.6;
    transition: opacity 0.3s ease;
    cursor: pointer;
    z-index: 20; /* Ensure it's above hero background */
  }

  /* Hide on mobile (scrolling is intuitive, saves space) */
  @media (max-width: 640px) {
    .scroll-indicator {
      display: none;
    }
  }

  .scroll-indicator:hover {
    opacity: 1;
  }

  /* Mouse Body */
  .mouse {
    width: 26px;
    height: 42px;
    border: 2px solid var(--color-text-muted);
    border-radius: 20px;
    position: relative;
    box-sizing: border-box;
  }

  /* Scrolling Wheel */
  .wheel {
    position: absolute;
    top: 6px;
    left: 50%;
    transform: translateX(-50%);
    width: 4px;
    height: 6px;
    background-color: var(--color-text); /* Brighter than border */
    border-radius: 2px;
    animation: scroll 2s infinite;
  }

  /* Arrows (Optional Chevron stack for extra cue) */
  .arrows {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: -4px;
    margin-top: 4px;
  }

  .arrows span {
    display: block;
    width: 10px;
    height: 10px;
    border-bottom: 2px solid var(--color-text-muted);
    border-right: 2px solid var(--color-text-muted);
    transform: rotate(45deg);
    animation: arrow 2s infinite;
    opacity: 0;
  }

  .arrows span:nth-child(2) {
    animation-delay: -0.2s;
  }

  .arrows span:nth-child(3) {
    animation-delay: -0.4s;
  }

  @keyframes scroll {
    0% {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }
    100% {
      opacity: 0;
      transform: translateX(-50%) translateY(12px);
    }
  }

  @keyframes arrow {
    0% {
      opacity: 0;
      transform: rotate(45deg) translate(-5px, -5px);
    }
    50% {
      opacity: 1;
    }
    100% {
      opacity: 0;
      transform: rotate(45deg) translate(5px, 5px);
    }
  }

  /* Hide on very short screens */
  @media (max-height: 600px) {
    .scroll-indicator {
      display: none;
    }
  }
</style>
