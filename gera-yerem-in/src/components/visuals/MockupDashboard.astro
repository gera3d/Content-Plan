<div class="mockup-dashboard">
    <div class="mockup-header">
        <div class="mockup-title"></div>
        <div class="mockup-actions">
            <div class="action-circle"></div>
            <div class="action-circle"></div>
        </div>
    </div>
    <div class="mockup-body">
        <div class="table-header">
            <div class="th th-1"></div>
            <div class="th th-2"></div>
            <div class="th th-3"></div>
        </div>
        <!-- Simulated Rows -->
        {Array.from({ length: 4 }).map((_, i) => (
            <div class="table-row" style={`animation-delay: ${i * 0.15 + 0.5}s`}>
                <div class="td td-1"></div>
                <div class="td td-2"></div>
                <div class="td td-status"></div>
            </div>
        ))}
    </div>
</div>

<style>
    /* Dark Mode Dashboard Mockup */
    .mockup-dashboard {
        width: 100%;
        max-width: 90%;
        background: linear-gradient(180deg, #2c2c2e 0%, #1c1c1e 100%);
        border-radius: 12px;
        box-shadow: 
            0 8px 32px rgba(0, 0, 0, 0.5),
            0 0 0 1px rgba(255, 255, 255, 0.1),
            inset 0 1px 0 rgba(255, 255, 255, 0.1);
        overflow: hidden;
        padding: 16px;
        position: relative;
    }

    .mockup-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        padding-bottom: 12px;
    }

    .mockup-title {
        height: 8px;
        width: 30%;
        background: rgba(255, 255, 255, 0.2); /* Muted white */
        border-radius: 4px;
    }

    .mockup-actions {
        display: flex;
        gap: 4px;
    }

    .action-circle {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
    }

    .table-header {
        display: flex;
        gap: 8px;
        margin-bottom: 8px;
    }

    .th {
        height: 6px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 3px;
    }
    .th-1 { width: 40%; }
    .th-2 { width: 30%; }
    .th-3 { width: 20%; }

    .table-row {
        display: flex;
        gap: 8px;
        margin-bottom: 8px;
        align-items: center;
        opacity: 0;
        animation: slideFadeIn 0.5s ease forwards;
    }

    .td {
        height: 6px;
        border-radius: 3px;
        background: rgba(255, 255, 255, 0.05);
    }
    .td-1 { width: 40%; height: 8px; background: rgba(255, 255, 255, 0.15); }
    .td-2 { width: 30%; }
    
    .td-status { 
        width: 20px; 
        height: 12px; 
        border-radius: 12px; 
        background: rgba(41, 151, 255, 0.3); /* Blue accent opacity */
        border: 1px solid rgba(41, 151, 255, 0.5);
    }

    @keyframes slideFadeIn {
        from {
            opacity: 0;
            transform: translateY(4px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
