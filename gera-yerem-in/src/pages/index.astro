---
import BaseLayout from '../layouts/BaseLayout.astro';
import FeatureGrid from '../components/content/FeatureGrid.astro';
import FeatureCard from '../components/content/FeatureCard.astro';
import BlogCard from '../components/BlogCard.astro';
import CtaRow from '../components/content/CtaRow.astro';
import TestimonialSlider from '../components/content/TestimonialSlider.astro';
import LogoBar from '../components/content/LogoBar.astro';
import FeaturedCaseStudies from '../components/content/FeaturedCaseStudies.astro';
import WhoIWorkWith from '../components/content/WhoIWorkWith.astro';
import AboutBio from '../components/content/AboutBio.astro';
import BentoGrid from '../components/content/BentoGrid.astro';
import BentoCard from '../components/content/BentoCard.astro';
import MockupDashboard from '../components/visuals/MockupDashboard.astro';
import MockupPortal from '../components/visuals/MockupPortal.astro';
import MockupPipeline from '../components/visuals/MockupPipeline.astro';
import HeroImage from '../components/visuals/HeroImage.astro';
import StatsBar from '../components/content/StatsBar.astro';

---

<BaseLayout 
  title="Home" 
  description="Stop manually copying data. Start building assets. I build custom software for businesses that have outgrown spreadsheets."
>
  <!-- Hero Section: Problem Focused -->
  <section class="hero">
    <div class="container">
      <div class="hero__content">
          <h1 class="hero__title">
            Stop running your business on<br />
            <span class="text-gradient">spreadsheets.</span>
          </h1>
          <p class="hero__subtitle">
            You've outgrown manual workarounds.<br />I build custom software that helps you scale—without the chaos.
          </p>
          
          <CtaRow>
            <a href="/contact" class="btn btn--primary">Book a Discovery Call</a>
            <a href="/work" class="btn btn--secondary">View My Work</a>
          </CtaRow>
      </div>
      <div class="hero__visual">
        <img src="/images/drake-meme.jpg" alt="Drake meme: No to manual data entry, Yes to custom software" />
      </div>
    </div>
  </section>


  <!-- Cost of Chaos -->
  <section class="cost-of-chaos section">
    <div class="container">
      <h2 class="section-title text-center">The Cost of Chaos</h2>
      <p class="section-subtitle text-center">These problems are costing you money every week you don't fix them.</p>
      
      <FeatureGrid columns={3}>
        <FeatureCard title="Scattered Data">
          <span slot="icon" class="chaos-icon">📊</span>
          <p>Sales in one spreadsheet, customers in another, inventory in your head. You can't answer basic questions without digging for 20 minutes.</p>
        </FeatureCard>

        <FeatureCard title="Expensive Busywork">
          <span slot="icon" class="chaos-icon">⏱️</span>
          <p>You're paying skilled people to copy-paste between systems. That's $50/hour labor doing $10/hour work.</p>
        </FeatureCard>

        <FeatureCard title="The Sarah Problem">
          <span slot="icon" class="chaos-icon">🔑</span>
          <p>Only one person knows how things really work. If they're sick, on vacation, or quit—you're screwed.</p>
        </FeatureCard>

        <FeatureCard title="Death by 1,000 Tools">
          <span slot="icon" class="chaos-icon">🔧</span>
          <p>You're paying for 8 different apps that don't talk to each other. None of them do exactly what you need.</p>
        </FeatureCard>

        <FeatureCard title="Bottleneck Central">
          <span slot="icon" class="chaos-icon">🔌</span>
          <p>Orders pile up. Approvals wait on you. Nothing moves unless you push it. You wanted a business, not a job.</p>
        </FeatureCard>

        <FeatureCard title="Gut Decisions">
          <span slot="icon" class="chaos-icon">🎯</span>
          <p>You're making $50K decisions based on instinct because pulling real numbers takes too long. That's gambling, not strategy.</p>
        </FeatureCard>
      </FeatureGrid>
    </div>
  </section>

  <!-- The Guide: Who I Work With (Qualifier) -->
  <WhoIWorkWith />

  <!-- The Plan: What I Build -->
  <section class="section">
    <div class="container">
      <h2 class="section-title text-center mb-12">What I Build</h2>
      <BentoGrid>
          <!-- Card 1: Operations Platform (Large) -->
          <BentoCard 
              title="Custom Operations Platforms" 
              subtitle="Stop reacting. Start orchestrating."
              colSpan={2}
              visualPosition="right"
          >
              <div slot="visual" style="width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;">
                  <MockupDashboard />
              </div>
              <p>Replace 20 scattered spreadsheets with one dashboard that tells you the truth about your business in real-time. Connects Sales, Ops, and Finance.</p>
          </BentoCard>

          <!-- Card 2: Client Portals (Tall) -->
          <BentoCard 
              title="Client Portals" 
              subtitle="Premium experience."
              rowSpan={2}
              visualPosition="top"
          >
              <div slot="visual" style="width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; padding: 2rem;">
                  <MockupPortal />
              </div>
              <p>Give your customers a white-labeled login to check project status, approve quotes, and view assets. Establish instant professional trust.</p>
          </BentoCard>

          <!-- Card 3: Workflow Automation (Small) -->
          <BentoCard 
              title="Workflow Automation" 
              subtitle="Zero human error."
              visualPosition="bottom"
          >
              <div slot="visual">
                  <MockupPipeline />
              </div>
              <p>Automate the boring stuff. Lead routing, invoice generation, and status updates happen while you sleep.</p>
          </BentoCard>
      </BentoGrid>
    </div>
  </section>

  <!-- The Guide: About Bio -->
  <AboutBio />

  <!-- The Guide: Credibility (Stats + LogoBar) -->
  <section class="section">
    <div class="container skin-guide">
        <div class="guide-content text-center" style="max-width: 800px; margin: 0 auto;">
             <!-- Quick Stats -->
            <div class="stats-grid mb-12">
                <div class="stat-item">
                    <span class="stat-number">15+</span>
                    <span class="stat-label">Years Exp</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">50+</span>
                    <span class="stat-label">Happy Clients</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">10x</span>
                    <span class="stat-label">Avg ROI</span>
                </div>
            </div>
            
             <LogoBar />
        </div>
    </div>
  </section>

  <!-- The Process: Simple 3-Step -->
  <section class="section section--alt">
    <div class="container text-center">
        <h2 class="section-title mb-12">Simple Process. Tangible Results.</h2>
        <div class="process-grid">
            <div class="process-step">
                <div class="step-number">01</div>
                <h3 class="step-title">Discovery</h3>
                <p class="text-muted">We map out your exact workflow and identify the bottlenecks. No sales pitch, just strategy.</p>
            </div>
            <div class="process-step">
                <div class="step-number">02</div>
                <h3 class="step-title">Build</h3>
                <p class="text-muted">I deliver a custom solution designed for your specific needs. No bloat, no unnecessary features.</p>
            </div>
            <div class="process-step">
                <div class="step-number">03</div>
                <h3 class="step-title">Launch</h3>
                <p class="text-muted">You get a system that works from day one. Your team gets their time back, and your business scales.</p>
            </div>
        </div>
    </div>
  </section>

  <!-- Social Proof: Carousel -->
  <section class="section">
    <div class="container">
      <h2 class="section-title text-center mb-12">Trusted by Leaders</h2>
      
      <TestimonialSlider count={6} />

    </div>
  </section>

    </div>
  </section>

  <!-- Recent Work Teaser -->
  <section class="section section--alt">
    <div class="container">
        <h2 class="section-title text-center mb-8">Selected Work</h2>
        <p class="text-center text-muted mb-12" style="max-width: 600px; margin: 0 auto;">
            Recent projects delivering measurable operational efficiency. From replacing spreadsheets to building full-scale operations platforms.
        </p>

        <FeaturedCaseStudies />

        <div class="text-center mt-12">
            <CtaRow>
                <a href="/work" class="btn btn--secondary" style="width: auto; margin: 0 auto;">View Full Portfolio →</a>
            </CtaRow>
        </div>
    </div>
  </section>

  <!-- Latest Writing -->
  <section class="section">
    <div class="container">
      <h2 class="section-title mb-8">Latest Strategy</h2>
      <div class="featured-grid">
          <BlogCard 
            title="Custom Business Software: The Complete Guide" 
            href="/custom-business-software-guide"
            topic="Software Development"
            image="/images/memes/meme-morpheus-excel.png"
          />
          <BlogCard 
            title="Custom Workflow Automation: When You've Outgrown Zapier" 
            href="/custom-workflow-automation-guide"
            topic="Workflow Automation"
            image="/images/memes/meme-afraid-zap-failed.png"
          />
      </div>
      
      <p class="text-center mb-4" style="margin-top: var(--space-8);">
        <a href="/">Browse all posts →</a>
      </p>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="section section--alt text-center">
    <div class="container" style="max-width: 600px;">
        <h2 class="section-title mb-6">Ready to build something better?</h2>
        <p class="text-xl text-muted mb-8">
            Let's talk about your situation. No sales pressure, just honest advice on whether custom software is the right move for you.
        </p>
        <a href="/contact" class="btn btn--primary">Book a 30-Minute Chat</a>
    </div>
  </section>

<style>
    /* Hero Styles */
    .hero {
        min-height: 100vh;
        padding: 10vh 0 8vh;
        display: flex;
        align-items: center;
        background: radial-gradient(ellipse at 70% 30%, rgba(99,102,241,0.05) 0%, transparent 60%);
    }

    .hero .container {
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        gap: 4rem;
        align-items: center;
        max-width: 1400px;
    }

    .hero__content {
        text-align: left;
    }

    .hero__title {
        font-size: clamp(2.5rem, 5vw, 4.5rem);
        font-weight: 900;
        line-height: 1.05;
        margin-bottom: var(--space-6);
        color: var(--color-text);
        letter-spacing: -0.04em;
        text-shadow: 0 4px 8px rgba(0,0,0,0.08);
        animation: fadeUp 0.6s ease-out both;
    }

    .text-gradient {
        color: var(--color-accent);
        background: linear-gradient(135deg, var(--color-accent) 0%, #3b82f6 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        filter: drop-shadow(0 2px 8px rgba(99,102,241,0.25));
    }

    .hero__subtitle {
        font-size: 1.25rem;
        color: var(--color-text-muted);
        margin-bottom: var(--space-8);
        margin-left: 0;
        line-height: 1.7;
        font-weight: 400;
        max-width: 540px;
        animation: fadeUp 0.6s ease-out 0.1s both;
    }

    .hero__trust {
        margin-top: 3rem;
        width: 100%;
        grid-column: 1 / -1;
        animation: fadeUp 0.6s ease-out 0.3s both;
    }

    .hero__visual {
        display: flex;
        justify-content: center;
        align-items: center;
        animation: fadeUp 0.8s ease-out 0.2s both;
    }

    .hero__visual img {
        max-width: 100%;
        width: 100%;
        margin: 0 auto;
        border-radius: 1.5rem;
        filter: drop-shadow(0 24px 48px rgba(0,0,0,0.18));
        transition: transform 0.4s ease;
    }

    .hero__visual img:hover {
        transform: scale(1.02);
    }

    /* Entrance Animation */
    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(24px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Cost of Chaos Section */
    .cost-of-chaos {
        padding: var(--space-16) 0;
        background: linear-gradient(180deg, rgba(248,250,252,0.5) 0%, transparent 100%);
    }

    .cost-of-chaos .section-title {
        margin-bottom: var(--space-3);
    }

    .cost-of-chaos .section-subtitle {
        color: var(--color-text-muted);
        font-size: 1.125rem;
        margin-bottom: var(--space-10);
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    .chaos-icon {
        font-size: 2rem;
        line-height: 1;
    }

    /* CTA Button Polish */
    .cta-row {
        animation: fadeUp 0.6s ease-out 0.2s both;
    }
    .btn {
        min-width: 200px;
        padding: 1rem 2rem;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .btn--primary {
        box-shadow: 0 4px 20px rgba(99,102,241,0.35), 0 0 0 0 rgba(99,102,241,0);
    }
    .btn--primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 28px rgba(99,102,241,0.45), 0 0 20px rgba(99,102,241,0.2);
    }
    .btn--secondary {
        background: transparent;
        border: 2px solid var(--color-border);
    }
    .btn--secondary:hover {
        background: rgba(99,102,241,0.1);
        border-color: var(--color-accent);
        color: var(--color-accent);
    }

    /* Featured Blog Grid */
    .featured-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: var(--space-8);
    }

    @media (max-width: 768px) {
        .hero {
             min-height: auto;
             padding: 6rem 0 4rem;
        }

        .hero .container {
            grid-template-columns: 1fr;
            text-align: left;
            gap: 2rem;
        }
        
        .hero__content {
            margin: 0 auto;
        }

        .hero__title {
            font-size: clamp(2rem, 6vw, 3rem);
        }

        .hero__subtitle {
            max-width: 100%;
        }

        .hero__visual {
            margin-top: var(--space-4);
            order: 2;
        }
        
        .hero__visual img {
            max-width: 320px;
        }

        .featured-grid {
            grid-template-columns: 1fr;
        }

        .cta-row {
            flex-direction: column;
            gap: 1rem;
        }
    }

    /* Skin: Problem Section (Visual Distinctiveness) */
    .skin-problem {
        background-color: var(--color-surface-alt, #f8fafc);
        border: 1px solid var(--color-border);
        border-radius: var(--border-radius);
        padding: var(--space-8);
        margin-top: var(--space-4);
    }
    
    /* Stats Grid */
    .stats-grid {
        display: flex;
        justify-content: center;
        gap: var(--space-12);
        margin-top: var(--space-8);
    }
    .stat-item {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .stat-number {
        font-size: var(--font-size-3xl);
        font-weight: 800;
        color: var(--color-accent);
    }
    .stat-label {
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--color-text-muted);
        font-weight: 600;
    }

    /* Process Grid */
    .process-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: var(--space-8);
        margin-top: var(--space-12);
    }
    .process-step {
        text-align: center;
        padding: var(--space-6);
        background: var(--color-surface);
        border: 1px solid var(--color-border);
        border-radius: var(--border-radius);
    }
    /* Number styling refinement */
    .step-number {
        font-size: 3rem;
        font-weight: 900;
        color: var(--color-surface-alt, #f1f5f9);
        line-height: 1;
        margin-bottom: var(--space-2);
        -webkit-text-stroke: 1px var(--color-border);
    }
    .step-title {
        font-size: var(--font-size-lg);
        font-weight: 700;
        margin-bottom: var(--space-4);
    }

    @media (max-width: 768px) {
        .stats-grid { gap: var(--space-6); }
        .stat-number { font-size: var(--font-size-2xl); }
        .process-grid { grid-template-columns: 1fr; }
    }
</style>
